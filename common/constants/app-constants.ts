export enum OperationType {
    CREATE = 'create',
    VIEW = 'view',
    UPDATE = 'update',
    DEFAULT = ''
}

export enum PAGE_CONFIG {
    CREATE = 'api/save',
    READ = 'getall',
}

export enum API_URL {
    default = '',
    login = '/products'
}